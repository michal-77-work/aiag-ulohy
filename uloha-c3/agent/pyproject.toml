[project]
name = "vendor-risk-agent"
version = "0.1.0"
description = "Plan-Execute vendor-risk agent (LangChain) using an MCP database tool and Tavily web search"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain[openai]>=1.3.14",
    "langchain-mcp-adapters>=0.1.0",
    "langchain-tavily>=0.2.0",
    "python-dotenv>=1.1.0",
]
